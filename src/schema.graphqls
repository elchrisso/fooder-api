type Food {
  id: Int!
  createdAt: String!
  updatedAt: String!
  name: String!
  description: String!
}

type Query {
  Food(id: Int!): Food
  allFoods: [Food]
}

type Mutation {
  createFood(name: String!, description: String!): Food
}
